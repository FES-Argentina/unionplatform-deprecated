import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  itemTitle: {
    fontSize: 24,
    fontWeight: 'bold',
    padding: 15,
  },
  itemName: {
    fontSize: 16,
    padding: 15,
  },
  itemPhoto: {
    height: 200,
    width: 'auto',
  }
});

export default styles;
