import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  label: {
    fontWeight: 'bold',
    paddingLeft: 30,
    paddingRight: 30,
  },
  value: {
    paddingLeft: 30,
    paddingBottom: 10,
    paddingRight: 30,
  },
});

export default styles;
