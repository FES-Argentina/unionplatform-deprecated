import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  message: {
  },
  title: {
    fontSize: 16,
  },
  body: {
    fontSize: 14,
  },
  closeButton: {
    position: 'absolute',
    right: 10,
    top: 10,
  },
});

export default styles;
