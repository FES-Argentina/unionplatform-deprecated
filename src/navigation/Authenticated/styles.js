import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  bars: {
    marginLeft: 15,
  },
  itemPhoto: {
    height: 200,
    width: 'auto',
  },
});

export default styles;
