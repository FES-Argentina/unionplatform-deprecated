import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  bars: {
    marginLeft: 15,
  },
  itemPhoto: {
    height: 200,
    width: 'auto',
  },
  footerLogo: {
    height: 50,
    width: 'auto',
    resizeMode: 'contain',
    marginTop: 10,
  },
});

export default styles;
